#
# Fortishield API configuration file
# Copyright (C) 2015, Fortishield Inc.
#
host: <%= @fortishield_api_host %>
port: <%= @fortishield_api_port %>
# Advanced configuration
https:
  enabled: <%= @fortishield_api_https_enabled %>
  key: <%= @fortishield_api_https_key %>
  cert: <%= @fortishield_api_https_cert %>
  use_ca: <%= @fortishield_api_https_use_ca %>
  ca: <%= @fortishield_api_https_ca %>
  ssl_protocol: <%= @fortishield_api_ssl_protocol %>
  ssl_ciphers: <%= @fortishield_api_ssl_ciphers %>
# Logging configuration
# Values for API log level: disabled, info, warning, error, debug, debug2 (each level includes the previous level).
logs:
  level: <%= @fortishield_api_logs_level %>
# Cross-origin resource sharing: https://github.com/aio-libs/aiohttp-cors#usage
cors:
  enabled: <%= @fortishield_api_cors_enabled %>
  source_route: <%= @fortishield_api_cors_source_route %>
  expose_headers: <%= @fortishield_api_cors_expose_headers %>
  allow_headers: <%= @fortishield_api_cors_allow_headers %>
  allow_credentials: <%= @fortishield_api_cors_allow_credentials %>
# Cache (time in seconds)
cache:
  enabled: <%= @fortishield_api_cache_enabled %>
  time: <%= @fortishield_api_cache_time %>
# Access parameters
access:
  max_login_attempts: <%= @fortishield_api_access_max_login_attempts %>
  block_time: <%= @fortishield_api_access_block_time %>
  max_request_per_minute: <%= @fortishield_api_access_max_request_per_minute %>
# Drop privileges (Run as ossec user)
drop_privileges: <%= @fortishield_api_drop_privileges %>
# Enable features under development
experimental_features: <%= @fortishield_api_experimental_features %>
# Enable remote commands
upload_configuration:
  remote_commands:
    localfile:
      allow: <%= @remote_commands_localfile %>
      exceptions: <%= @remote_commands_localfile_exceptions %>
    wodle_command:
      allow: <%= @remote_commands_wodle %>
      exceptions: []
  limits:
    eps:
      allow: <%= @limits_eps %>
